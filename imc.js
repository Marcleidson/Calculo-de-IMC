const nome = 'Carlos'
const peso = 84
const altura = 1.88

imc = peso / (altura * altura)

function calc () {
  if (imc >= 30) {
    console.log ('Carlos você está acima do peso.')
  } else 
    cosole.log ('Carlos você não está acima do peso')
  }
